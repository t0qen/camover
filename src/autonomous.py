import time

def start_autonomous(robot):
    pass
    # while True:
        # 1. Capture une image
        # frame = robot.get_camera_frame()

        # 2. Envoie l'image à Ollama (à implémenter)
        # instructions = send_to_ollama(frame)

        # 3. Exécute les instructions (ex: "avancer", "tourner")
        # if instructions == "avancer":
            # robot.move("forward", 0.5)
        # elif instructions == "tourner":
            # robot.move("turn_left", 0.5)

        # 4. Attend avant la prochaine itération
        # time.sleep(1)